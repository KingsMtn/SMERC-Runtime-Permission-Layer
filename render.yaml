services:
  - type: web
    name: smerc-recoverability-api
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python api_server.py --host 0.0.0.0 --port $PORT"
    healthCheckPath: "/health"
